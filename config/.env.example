# EduBank Konfiguration
# Kopiere diese Datei nach .env und passe die Werte an
# cp .env.example .env

# Datenbank
MYSQL_URL=localhost
MYSQL_USERNAME=edubank
MYSQL_PASSWORD=dein_sicheres_passwort
MYSQL_DATABASE=edubank

# Sicherheit - UNBEDINGT ÄNDERN!
# Generiere einen zufälligen String mit: openssl rand -hex 32
SECURITY_SALT=hier_einen_langen_zufaelligen_string_eintragen

# Standard-Passwörter für neue Benutzer
DEFAULT_ADMIN_PASSWORD=SchulAdmin2024
DEFAULT_USER_PASSWORD=Schueler2024

# Debug-Modus (false für Produktion!)
DEBUG=false

# E-Mail-Konfiguration
# Absenderadresse
EMAIL_FROM=noreply@edubank.at

# SMTP-Konfiguration (optional)
# Wenn SMTP_HOST gesetzt ist, wird SMTP verwendet
# Wenn nicht gesetzt, wird PHP mail() verwendet
#SMTP_HOST=smtp.example.com
#SMTP_PORT=587
#SMTP_USER=dein_smtp_user
#SMTP_PASS=dein_smtp_passwort
#SMTP_TLS=true
